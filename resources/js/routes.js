import Router from 'vue-router';
import Vue from 'vue';
Vue.use(Router);

const routes = [
    {
       //routes 
    }
]

const router = new Router({
	mode: 'history',
	routes: routes
});

export default router
